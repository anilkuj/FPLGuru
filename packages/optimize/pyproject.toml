[project]
name = "fplguru-optimize"
version = "0.0.0"
description = "Pure squad optimiser: best XI, transfer suggestions, chip-timing hints."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fplguru_optimize"]
