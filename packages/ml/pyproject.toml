[project]
name = "fplguru-ml"
version = "0.0.0"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2,<2.5",
    "pandas>=2.2,<4",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fplguru_ml"]
