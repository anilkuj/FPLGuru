[project]
name = "fplguru-alerts"
version = "0.0.0"
description = "Pure alert generators + priority scoring."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fplguru_alerts"]
