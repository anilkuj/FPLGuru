[project]
name = "fplguru-explain"
version = "0.0.0"
description = "Pure helpers: turn Advanced-xP drivers into an LLM prompt or a template string."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fplguru_explain"]
