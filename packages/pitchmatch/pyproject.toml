[project]
name = "fplguru-pitchmatch"
version = "0.0.0"
description = "Pure PitchAPI <-> FPL identity matching + match xG normalization."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fplguru_pitchmatch"]
