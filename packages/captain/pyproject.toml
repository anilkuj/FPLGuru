[project]
name = "fplguru-captain"
version = "0.0.0"
description = "Pure captain-pick ranking + LLM prompt building."
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fplguru_captain"]
